import React from 'react';

import '../../../../../styles/globals.css';

export const metadata = {
    title: 'Edit Job | Joobify',
    description: 'Joobify Edit Job Page',
}

export default function UpdateJobLayout({ children }) {
    return (
        <div>
            {children}
        </div>
    )
}
