import React from 'react';

import '../../../styles/globals.css';

export const metadata = {
    title: 'Edit Profile | Joobify',
    description: 'Joobify Edit User Profile Page',
}

export default function EditProfileLayout({ children }) {
    return (
        <div>
            {children}
        </div>
    )
}
